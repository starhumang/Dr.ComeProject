<?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.drcome.project.common.mapper.UserMemberMapper">
	<select id="selectMemberList"
		resultType="com.drcome.project.common.service.UserMemberVO">
		SELECT * FROM usertable
	</select>
	
	  <insert id="insertUserMember" parameterType="com.drcome.project.common.service.UserMemberVO">
        <![CDATA[
        INSERT INTO usertable (user_id, user_pw, user_name, phone, birthday, gender)
        VALUES
        (#{userId}, #{userPw}, #{userName}, #{phone}, #{birthday}, #{gender})
        ]]>
    </insert>
    
    <select id="selectUser" parameterType="com.drcome.project.common.service.UserMemberVO" resultType="com.drcome.project.common.service.UserMemberVO">
		SELECT user_id, user_pw, user_name, phone, birthday, gender, joindate, 'ROLE_'||grade as grade
		FROM usertable
		<if test="userId != null and userId != ''">WHERE user_id=#{userId}</if>
	</select>
</mapper>